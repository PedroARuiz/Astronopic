import SwiftUI
import core

struct ContentView: View {
    var body: some View {
        PruebaKMP().mySuspend()
        Text("\()")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
